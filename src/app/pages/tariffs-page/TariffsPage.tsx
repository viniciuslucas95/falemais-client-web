import { PageContainer, PageProps } from "../PageContainer";
import styled from "styled-components";

export function TariffsPage({ plans, tariffs }: PageProps) {
    return <PageContainer>
        <Main>

        </Main>
    </PageContainer>
}

const Main = styled.main`
    
`